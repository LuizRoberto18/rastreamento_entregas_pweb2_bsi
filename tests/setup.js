// Configurações globais que rodam ANTES dos testes de integração (ex: hooks globais)
// Para os testes unitários, garante que não haverá efeitos colaterais.
beforeAll(async () => {
    // Configurações globais se necessário
});

afterAll(async () => {
    // Encerramento de conexões globais se necessário
});